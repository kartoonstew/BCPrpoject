import {abilities,skills,skillAbilities,defaults,modifier,signed,derived,validate,sanitizeCharacter} from './character.js';
import {createCharacterPDF} from './pdf-generator.js';
const main=document.querySelector('#main');
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const icon=(name='book')=>{const paths={book:'M4 4h6c2 0 2 2 2 2s0-2 2-2h6v15h-6c-2 0-2 2-2 2s0-2-2-2H4z M12 6v15',search:'M21 21l-6-6 M17 10a7 7 0 1 0-14 0 7 7 0 0 0 14 0',sword:'M5 3l14 14 M3 5l14 14 M4 4l4 1-3 3z M14 20l6-6 M17 17l4 4',banner:'M5 22V3 M5 4h15l-4 5 4 5H5',shield:'M12 3l8 3v7c0 5-8 9-8 9s-8-4-8-9V6z M12 7v10 M8 12h8',people:'M9 11a4 4 0 1 0 0-8 4 4 0 0 0 0 8 M2 22v-4a7 7 0 0 1 14 0v4 M16 3a4 4 0 0 1 0 8 M18 14a6 6 0 0 1 4 5v3',box:'M3 7l9-4 9 4-9 4z M3 7v12l9 4 9-4V7 M12 11v12',star:'M12 2l3 7 7 3-7 3-3 7-3-7-7-3 7-3z'};return `<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="${paths[name]||paths.book}"/></svg>`;};
let DB,character,bookmarks=new Set(),limit=35;
let toastTimer;
function toast(message){const el=document.querySelector('#toast');el.textContent=message;el.classList.add('visible');clearTimeout(toastTimer);toastTimer=setTimeout(()=>el.classList.remove('visible'),3500);}
const route=()=>{const [path,query='']=(location.hash.slice(1)||'home').split('?');return {path,params:new URLSearchParams(query)};};
const categoryLink=cat=>`#rules?category=${encodeURIComponent(cat)}`;
const ruleLink=id=>`#rule/${encodeURIComponent(id)}`;
const record=id=>DB.records.find(r=>r.id===id);
const normal=s=>String(s).toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g,'').replace(/[’']/g,'').replace(/[^a-z0-9]+/g,' ').trim();
const categories=['Character creation','Subclasses','Traits','Quirks','Black Banner','Morale','Commissary','Company lore','Brothers'];
const searchMarkup=(q='',id='rules-search')=>`<div class="search-box">${icon('search')}<input type="search" id="${id}" aria-label="Search all rules" placeholder="Search rules, abilities, or a brother’s name…" value="${esc(q)}" autocomplete="off"><kbd>/</kbd></div>`;
function header(eyebrow,title,description,action=''){return `<div class="page-top"><div><div class="eyebrow">${eyebrow}</div><h1>${title}</h1><p>${description}</p></div>${action}</div>`;}
function home(){const chapters=[['01','Character creation','Make your mark. Build a Brother.','shield'],['02','Subclasses','Nine ways to hold the line.','sword'],['03','Traits','The gifts and flaws you carry.','star'],['04','Black Banner','Stand together. Stay alive.','banner'],['05','Morale','Know when the enemy will break.','people'],['06','Commissary','Steel, spoils, and borrowed magic.','box']];main.innerHTML=`<section class="hero"><img class="hero-art" src="assets/art/last-of-the-free.webp" alt="A battle-worn mercenary and his brothers beneath a ragged black banner on a smoke-filled battlefield" fetchpriority="high"><div class="hero-copy"><div class="eyebrow">Last of the Free Companies of Khatovar</div><h1>Live by the sword.<br><em>Die a Brother.</em></h1><p>The rules of the road. The men beside you.<br>Everything you need to carry the Black.</p><div class="hero-actions"><a class="button primary" href="#rules">Open the field manual <span>↗</span></a><a class="text-link" href="#sheet">Create a Brother <span>→</span></a></div></div><div class="hero-caption">THE ANNALS / PLAYER’S HANDBOOK 1.14</div></section><div class="home-body"><section class="search-block" aria-label="Find a rule"><div class="search-heading"><h2>A rule for the moment.</h2><span class="edition">V1.14 · ${DB.records.length} INDEXED ENTRIES</span></div><form id="home-search-form">${searchMarkup('','home-search')}</form><div class="quick-links"><span>QUICK LOOKUP</span><a href="${ruleLink('subclass-saving-throw-dc')}">Save DC</a><a href="${ruleLink('morale-checks-outcomes')}">Morale checks</a><a href="${ruleLink('aura-of-brotherhood')}">Brotherhood aura</a><a href="${categoryLink('Quirks')}">Quirks</a></div></section><div class="section-heading"><div><div class="eyebrow">Know your ground</div><h2>The field manual</h2></div><a class="text-link" href="#rules">View full index <span>→</span></a></div><div class="chapter-grid">${chapters.map(([n,c,d,i])=>`<a class="chapter" href="${categoryLink(c)}"><div class="chapter-top"><span>CHAPTER ${n}</span>${icon(i)}</div><h3>${c==='Traits'?'Traits & quirks':c==='Subclasses'?'The brothers':c==='Black Banner'?'The Black Banner':c==='Commissary'?'The Commissary':c}</h3><p>${d}</p><span class="arrow">↗</span></a>`).join('')}</div><section class="sheet-promo"><div><div class="eyebrow">Your name in the Annals</div><h2>No two Brothers.<br>The same road.</h2><p>One character sheet for every calling. Choose your subclass, record your scars, and take an editable PDF to the table.</p><a class="text-link" href="#sheet">Create your character sheet <span>→</span></a></div><a class="paper-preview" href="#sheet" aria-label="Create a character sheet"><div class="preview-title"><small>THE BLACK COMPANY · SERVICE RECORD</small>A Brother of the Company</div><div class="preview-fields"><span>CALLING<b>Salt</b></span><span>LEVEL<b>07</b></span><span>SAVE DC<b>15</b></span></div><div class="preview-fields"><span>STRENGTH<b>16</b></span><span>DEXTERITY<b>12</b></span><span>CONSTITUTION<b>14</b></span></div></a></section></div>`;document.querySelector('#home-search-form').addEventListener('submit',e=>{e.preventDefault();location.hash=`rules?q=${encodeURIComponent(document.querySelector('#home-search').value)}`;});let t;document.querySelector('#home-search').addEventListener('input',e=>{clearTimeout(t);const q=e.target.value;t=setTimeout(()=>{if(q.trim()){location.hash=`rules?q=${encodeURIComponent(q)}`;requestAnimationFrame(()=>document.querySelector('#rules-search')?.focus());}},450);});}
function searchRecords(q,cat,saved,sort){let normalized=normal(q).replace(/death saves?/g,'death checks').replace(/saving throw dc/g,'saving throw dc');const tokens=normalized.split(' ').filter(Boolean);let rows=DB.records.filter(r=>(!cat||r.category===cat)&&(!saved||bookmarks.has(r.id))).map(r=>{const title=normal(r.title),text=normal(`${r.title} ${r.category} ${r.subclass||''} ${r.body}`);let score=tokens.every(t=>text.includes(t))?1:0;if(!score)return null;if(title===normalized)score+=100;else if(normalized&&title.includes(normalized))score+=40;score+=tokens.filter(t=>title.includes(t)).length*10;return {r,score};}).filter(Boolean);rows.sort((a,b)=>sort==='az'?a.r.title.localeCompare(b.r.title):sort==='page'?a.r.page-b.r.page:b.score-a.score);return rows.map(x=>x.r);}
function highlight(text,q){const words=q.trim().split(/\s+/).filter(t=>t.length>1);if(!words.length)return esc(text);const re=new RegExp(`(${words.map(w=>w.replace(/[.*+?^${}()|[\]\\]/g,'\\$&')).join('|')})`,'gi');return String(text).split(re).map((part,i)=>i%2?`<mark>${esc(part)}</mark>`:esc(part)).join('');}
function row(r,q=''){return `<a class="result-row" href="${ruleLink(r.id)}"><div><div class="result-meta">${esc(r.category)}${r.subclass?' / '+esc(r.subclass):''}${r.level?' / LEVEL '+r.level:''}</div><h3>${highlight(r.title,q)}</h3><p>${highlight(r.body,q)}</p></div><div class="row-end"><span>P. ${r.page}${r.endPage!==r.page?'–'+r.endPage:''}</span><span>↗</span></div></a>`;}
function rules(){const {params}=route(),cat=params.get('category')||'',q=params.get('q')||'';limit=35;main.innerHTML=`<div class="page-wrap">${header('The Annals / Rules reference','The rules index.','Find the rule. Follow the source. Every campaign entry from Player’s Handbook v1.14.',`<a class="button" href="BC%20PHB%201.14.pdf" target="_blank" rel="noopener">Read original PDF <span>↗</span></a>`)}<div class="index-layout"><aside class="filter-sidebar" aria-label="Filter rules"><div class="sidebar-label">CHAPTERS</div><div class="filter-list"><button class="filter ${!cat&&!params.has('saved')?'active':''}" data-category="">All entries <span>${DB.records.length}</span></button>${categories.map(c=>`<button class="filter ${cat===c?'active':''}" data-category="${c}">${c}<span>${DB.records.filter(r=>r.category===c).length}</span></button>`).join('')}<button class="filter ${params.has('saved')?'active':''}" data-saved="true">Saved rules <span>${bookmarks.size}</span></button></div><p class="sidebar-note">A companion to your PHB.<br><br>This booklet supplies campaign changes; the Fighter table and general PHB rules remain separate.<br><br><a class="text-link" href="#about">Edition notes ↗</a></p></aside><section aria-label="Search results">${searchMarkup(q)}<div class="results-controls"><span id="result-count" role="status"></span><label>Sort <select id="sort"><option value="relevance">Relevance</option><option value="az">A–Z</option><option value="page">Source page</option></select></label></div><div id="results" class="result-list"></div><div id="load-more" class="load-more"></div></section></div></div>`;document.querySelector('#sort').value=params.get('sort')||'relevance';const update=()=>{const p=route().params;p.set('q',document.querySelector('#rules-search').value);p.set('sort',document.querySelector('#sort').value);history.replaceState(null,'',`#rules?${p}`);limit=35;renderResults();};document.querySelector('#rules-search').addEventListener('input',update);document.querySelector('#sort').addEventListener('change',update);document.querySelectorAll('[data-category]').forEach(b=>b.addEventListener('click',()=>{const p=route().params;p.delete('saved');p.set('category',b.dataset.category);location.hash=`rules?${p}`;}));document.querySelector('[data-saved]').addEventListener('click',()=>{const p=route().params;p.delete('category');p.set('saved','1');location.hash=`rules?${p}`;});renderResults();}
function renderResults(){const p=route().params,q=p.get('q')||'',rows=searchRecords(q,p.get('category'),p.has('saved'),p.get('sort'));document.querySelector('#result-count').textContent=`${rows.length} ${rows.length===1?'entry':'entries'}${q?' for “'+q+'”':''}`;document.querySelector('#results').innerHTML=rows.length?rows.slice(0,limit).map(r=>row(r,q)).join(''):`<div class="empty"><h3>${p.has('saved')?'Your field notes start here.':'No entries found.'}</h3><p>${p.has('saved')?'Open a rule and choose “Save rule” to keep it here.':'Try a shorter term or another chapter. The full Fighter rules are in your PHB.'}</p><a class="button" href="#rules">Browse all entries</a></div>`;const load=document.querySelector('#load-more');load.innerHTML=rows.length>limit?`<button class="button">Show more entries (${rows.length-limit} remaining)</button>`:'';load.querySelector('button')?.addEventListener('click',()=>{limit+=35;renderResults();});}
function rule(id){const r=record(id);if(!r)return notFound();const related=DB.records.filter(x=>x.id!==id&&(r.subclass?x.subclass===r.subclass:x.category===r.category)).slice(0,9);const paragraphs=r.body.replace(/ (\d\. )/g,'\n\n$1');main.innerHTML=`<div class="reader-layout"><div><a class="breadcrumb" href="${categoryLink(r.category)}">← ${esc(r.category)} / Rules index</a><article class="reader-paper"><div class="eyebrow">${esc(r.subclass||r.category)}${r.level?' · Level '+r.level:''} · P. ${r.page}</div><h1>${esc(r.title)}</h1><div class="rule-body"><p>${esc(paragraphs)}</p></div>${r.note?`<aside class="source-note">${esc(r.note)}</aside>`:''}<div class="reader-actions"><button id="bookmark" class="button light" aria-pressed="${bookmarks.has(id)}">${bookmarks.has(id)?'★ Saved rule':'☆ Save rule'}</button><button id="copy-link" class="button light">Copy link</button><a class="button light" href="BC%20PHB%201.14.pdf#page=${r.page}" target="_blank" rel="noopener">Source · p. ${r.page} ↗</a></div></article></div><aside class="reader-aside"><div class="eyebrow">Keep reading</div><h3>In this chapter</h3>${related.map(x=>`<a class="related-link" href="${ruleLink(x.id)}">${esc(x.title)}<small>${x.level?'Level '+x.level+' · ':''}Page ${x.page}</small></a>`).join('')}<p class="sidebar-note">Source wording is preserved. Editorial notes are labeled separately.</p></aside></div>`;document.querySelector('#bookmark').addEventListener('click',()=>{bookmarks.has(id)?bookmarks.delete(id):bookmarks.add(id);try{localStorage.setItem('bc-saved-rules',JSON.stringify([...bookmarks]));}catch{toast('Browser storage is unavailable; saved for this visit.');}const b=document.querySelector('#bookmark');b.textContent=bookmarks.has(id)?'★ Saved rule':'☆ Save rule';b.setAttribute('aria-pressed',bookmarks.has(id));});document.querySelector('#copy-link').addEventListener('click',async()=>{try{await navigator.clipboard.writeText(location.href);toast('Rule link copied.');}catch{toast('Copy the link from your browser’s address bar.');}});}
function subclasses(){main.innerHTML=`<div class="page-wrap">${header('Nine callings. One Company.','The brothers.','Every Brother starts as a Fighter. Choose your calling at level 1; new subclass features arrive at levels 3, 7, and 10.')}<div class="class-grid">${DB.classes.map((c,i)=>`<a class="class-card" href="#class/${c.name}"><span class="class-number">${String(i+1).padStart(2,'0')}</span><h2>${c.name}</h2><p>${c.description}</p><span class="text-link">Explore the ${c.name} <span>↗</span></span></a>`).join('')}</div><div class="section-heading"><h2>The men behind the names.</h2><a class="text-link" href="${categoryLink('Brothers')}">Meet 75 Brothers →</a></div><p class="muted">Explore the Company’s <a href="${categoryLink('Company lore')}" class="text-link">leadership and lore ↗</a></p></div>`;}
function classDetail(name){const c=DB.classes.find(c=>c.name===name);if(!c)return notFound();const features=DB.records.filter(r=>r.subclass===name&&r.level);main.innerHTML=`<div class="page-wrap"><a class="breadcrumb" href="#subclasses">← All nine subclasses</a>${header('Fighter subclass / '+name,name+'.',c.description,`<button id="choose-class" class="button primary">Create a ${name} <span>↗</span></button>`)}<p class="muted">${['Sawbones','Bump','Deacon','Salt'].includes(name)?'Heavy armor training, in addition to light and medium armor and shields.':'Light and medium armor training, and shields.'} Subclass save DC = 9 + twice your proficiency bonus.</p><div class="class-levels">${features.map(r=>`<a href="${ruleLink(r.id)}"><small>LEVEL ${r.level}</small><h3>${r.title}</h3></a>`).join('')}</div><div class="result-list" style="margin-top:32px">${features.map(r=>row(r)).join('')}</div></div>`;document.querySelector('#choose-class').addEventListener('click',()=>{character.subclass=name;saveCharacter();location.hash='sheet';});}
const field=(key,label,type='text',opts={})=>`<div class="field ${opts.full?'full':''} ${opts.className||''}"><label for="${key}">${label}</label>${type==='textarea'?`<textarea id="${key}" name="${key}" rows="${opts.rows||3}" maxlength="12000">${esc(character[key])}</textarea>`:type==='select'?`<select id="${key}" name="${key}">${opts.options.map(o=>`<option value="${esc(o)}" ${character[key]===o?'selected':''}>${esc(o||'— None —')}</option>`).join('')}</select>`:`<input id="${key}" name="${key}" type="${type}" value="${esc(character[key])}" ${opts.min!==undefined?`min="${opts.min}"`:''} ${opts.max!==undefined?`max="${opts.max}"`:''} ${type==='number'?'step="1" required':''} ${type==='text'?'maxlength="180"':''}>`}${opts.help?`<small class="mod" id="${key}-mod">${opts.help}</small>`:''}</div>`;
function sheet(){main.innerHTML=`<div class="page-wrap">${header('The muster roll / Character workshop','Write your name.','A universal service record for all nine subclasses. Your draft saves in this browser; export a backup to keep it elsewhere.')}<div class="sheet-layout"><form id="character-form" class="sheet-form"><fieldset class="form-section"><legend>01 / The Brother</legend><div class="fields">${field('name','Company name')}${field('player','Player')}${field('subclass','Subclass','select',{options:DB.classes.map(c=>c.name)})}${field('level','Fighter level','number',{min:1,max:20})}${field('companyUnit','Fist / Finger / Knuckle')}${field('pronouns','Pronouns / description')}</div></fieldset><fieldset class="form-section"><legend>02 / Abilities & training</legend><div class="fields six">${abilities.map(a=>field(a.toLowerCase(),a.slice(0,3),'number',{min:1,max:30,className:'ability-field',help:signed(modifier(character[a.toLowerCase()]))})).join('')}</div><p class="field-help">Enter final scores after your chosen increases: +2 / +1, or +1 to three scores. Those increases cannot take a score above 16 at level 1.</p><div class="fields" style="margin-top:20px">${field('save1','Saving throw proficiency I','select',{options:['Strength','Intelligence','Charisma']})}${field('save2','Saving throw proficiency II','select',{options:['Constitution','Dexterity','Wisdom']})}${field('extraSaves','Additional saving throw proficiencies','text',{full:true})}</div><p class="field-help" id="skill-count">Choose five skill proficiencies; Skill Monkey grants two more.</p><div class="check-grid" style="margin:15px 0">${skills.map(s=>`<label class="check-label"><input type="checkbox" name="skills" value="${s}" ${character.skills.includes(s)?'checked':''}>${s}</label>`).join('')}</div><div class="fields">${field('tools','Tool proficiencies')}${field('languages','Languages')}</div></fieldset><fieldset class="form-section"><legend>03 / Gifts & scars</legend><div class="fields three">${field('trait','First Trait','select',{options:['',...DB.records.filter(r=>r.category==='Traits').map(r=>r.title)]})}${field('trait2','Second Trait (optional)','select',{options:['',...DB.records.filter(r=>r.category==='Traits').map(r=>r.title)]})}${field('quirk','Quirk','select',{options:['',...DB.records.filter(r=>r.category==='Quirks').map(r=>r.title)]})}${field('traitChoices','Trait choices, resistances & penalties','textarea',{full:true})}</div><p class="field-help">A second Trait requires a Quirk. Record Fey-Touched saves, Odd Fellow / Sponge damage types, and other choices here. Add any extra save proficiency above.</p><div id="trait-reference" class="feature-summary"></div></fieldset><fieldset class="form-section"><legend>04 / In the line</legend><div class="fields three">${[['hpMax','Maximum HP'],['hpCurrent','Current HP'],['hpTemp','Temporary HP'],['ac','Armor class'],['speed','Base speed (ft)'],['initiative','Initiative override'],['hitDice','Hit dice remaining'],['luck','Session Luck'],['chips','Chips'],['secondWind','Second Wind uses'],['actionSurge','Action Surge uses'],['indomitable','Indomitable uses']].map(([k,l])=>field(k,l,k==='speed'?'number':'text',k==='speed'?{min:0,max:200}:{})).join('')}${field('deathSuccesses','Death checks: successes','number',{min:0,max:3})}${field('deathFailures','Death checks: failures','number',{min:0,max:3})}${field('gold','Gold remaining','number',{min:0})}${field('resources','Subclass resources — current / maximum','textarea',{full:true})}</div><p class="field-help">HP, armor class, equipment bonuses, and Fighter resources are entered manually. Speed includes Fast / Slow; the initiative default includes Snake. Temporary effects and other trait penalties are not applied automatically.</p></fieldset><fieldset class="form-section"><legend>05 / Steel & spoils</legend><div class="fields">${field('weapons','Weapons · attack bonus · damage · mastery','textarea',{full:true,rows:4})}${field('armor','Armor & shield')}${field('magicItems','Magic items · DP / charges')}${field('equipment','Equipment & supplies','textarea',{full:true,rows:4})}</div></fieldset><fieldset class="form-section"><legend>06 / The Annals</legend><div class="fields">${field('history','Before the Company','textarea',{full:true})}${field('allies','Brothers, bonds & enemies','textarea',{full:true})}${field('notes','Field notes & conditions','textarea',{full:true})}${field('featureNotes','Fighter features, masteries & other rules','textarea',{full:true})}</div></fieldset></form><aside class="sheet-aside"><h2>Your service record</h2><p>Download a filled sheet with editable fields, or a blank universal sheet for the table.</p><div id="save-status" class="status-line" role="status">Draft stored on this device</div><button id="download-pdf" class="button primary">Download editable PDF <span>↓</span></button><button id="blank-pdf" class="button">Blank universal PDF <span>↓</span></button><div class="two-actions"><button id="export-json" class="button">Export backup</button><button id="import-json" class="button">Import backup</button></div><input class="file-input" type="file" accept="application/json,.json" id="import-file"><div id="derived-stats" class="derived-stats"></div><div id="validation"></div><div id="feature-summary" class="feature-summary"></div><p class="field-help">PDF fields stay editable in compatible viewers, including Adobe Acrobat Reader. Backup files preserve your web draft.</p></aside></div></div>`;document.querySelector('#character-form').addEventListener('submit',e=>e.preventDefault());document.querySelector('#character-form').addEventListener('input',e=>{const el=e.target;if(!el.name)return;if(el.name==='skills')character.skills=[...document.querySelectorAll('[name="skills"]:checked')].map(x=>x.value);else character[el.name]=el.type==='number'?(el.value===''?'':Number(el.value)):el.value;saveCharacter();updateSheet();});document.querySelector('#download-pdf').addEventListener('click',()=>downloadPDF(false));document.querySelector('#blank-pdf').addEventListener('click',()=>downloadPDF(true));document.querySelector('#export-json').addEventListener('click',()=>{download(JSON.stringify({format:'black-company-character',version:1,character},null,2),`${fileName()}-backup.json`,'application/json');toast('Character backup exported.');});document.querySelector('#import-json').addEventListener('click',()=>document.querySelector('#import-file').click());document.querySelector('#import-file').addEventListener('change',async e=>{const file=e.target.files[0];if(!file)return;try{if(file.size>500000)throw new Error('This file is too large. Choose a character JSON backup.');const parsed=JSON.parse(await file.text());if(parsed.format!=='black-company-character'||parsed.version!==1||!parsed.character)throw new Error('Choose a Black Company character backup.');const next=sanitizeCharacter(parsed.character,DB.classes.map(c=>c.name),DB.records.filter(r=>r.category==='Traits').map(r=>r.title),DB.records.filter(r=>r.category==='Quirks').map(r=>r.title));download(JSON.stringify({format:'black-company-character',version:1,character},null,2),`${fileName()}-before-import.json`,'application/json');character=next;saveCharacter();sheet();toast('Backup imported. The previous draft was downloaded for safekeeping.');}catch(error){toast(error.message);}e.target.value='';});updateSheet();}
function updateSheet(){const c=character,d=derived(c);for(const a of abilities){document.querySelector(`#${a.toLowerCase()}-mod`).textContent=signed(modifier(c[a.toLowerCase()]));}document.querySelector('#derived-stats').innerHTML=[[signed(d.pb),'Proficiency'],[d.dc,'Subclass save DC'],[d.hitDie,'Hit point die'],[`${d.speed} ft`,'Travel speed']].map(([v,l])=>`<div><b>${v}</b><small>${l}</small></div>`).join('');const problems=validate(c);document.querySelector('#validation').innerHTML=problems.length?`<div class="validation"><strong>Before you muster</strong><br>${problems.map(esc).join('<br>')}</div>`:'';document.querySelector('#feature-summary').innerHTML=`<div class="sidebar-label">${esc(c.subclass)} · LEVEL ${esc(c.level)}</div>${DB.records.filter(r=>r.subclass===c.subclass&&r.level<=Number(c.level)).map(r=>`<a href="${ruleLink(r.id)}">${r.title}<small>Level ${r.level} · Read feature ↗</small></a>`).join('')}`;document.querySelector('#trait-reference').innerHTML=[c.trait,c.trait2,c.quirk].filter(Boolean).map(title=>{const r=DB.records.find(r=>r.title===title);return r?`<a href="${ruleLink(r.id)}">${esc(r.title)}<small>${esc(r.body)}</small></a>`:'';}).join('');document.querySelector('#skill-count').textContent=`${c.skills.length} selected · Choose ${c.trait==='Skill Monkey'||c.trait2==='Skill Monkey'?7:5} skill proficiencies.`;}
function saveCharacter(){try{localStorage.setItem('bc-character-v1',JSON.stringify(character));const s=document.querySelector('#save-status');if(s)s.textContent='Draft saved on this device';}catch{const s=document.querySelector('#save-status');if(s)s.textContent='Storage unavailable — export a backup before leaving.';}}
function fileName(){return (character.name||'black-company-brother').replace(/[^a-zA-Z0-9_-]+/g,'-').slice(0,70);}
function download(data,name,type){const url=URL.createObjectURL(new Blob([data],{type})),a=document.createElement('a');a.href=url;a.download=name;a.click();setTimeout(()=>URL.revokeObjectURL(url),30000);}
async function downloadPDF(blank){const button=document.querySelector(blank?'#blank-pdf':'#download-pdf');if(!blank&&!document.querySelector('#character-form').reportValidity())return;button.disabled=true;const original=button.innerHTML;button.textContent='Preparing your record…';try{const bytes=await createCharacterPDF(character,DB,{blank});download(bytes,blank?'black-company-universal-editable.pdf':`${fileName()}-editable.pdf`,'application/pdf');toast('Editable PDF downloaded.');}catch(e){console.error(e);toast(/WinAnsi|encode/i.test(e.message)?'The PDF font cannot display a character in your text. Use Latin characters for PDF export; your full draft and JSON backup are preserved.':'Could not create the PDF. Your draft is safe; please try again.');}finally{button.disabled=false;button.innerHTML=original;}}
function about(){main.innerHTML=`<div class="page-wrap about-content">${header('The source of the Annals','Sources & edition notes.','The supplied Player’s Handbook v1.14 is the authority for this field manual.')}<h2>A complete campaign index</h2><p>The ${DB.records.length} entries cover all campaign rules and lore on pages 1–8. Pages 9–10 are Homebrewery help and are excluded from the index. Every entry links directly to its source page. Line wrapping is normalized; source wording, including occasional typos, is retained.</p><p><a href="BC%20PHB%201.14.pdf" target="_blank" rel="noopener">Read the original v1.14 rulebook ↗</a> · <a href="data/rules.json" download>Download the structured index</a></p><h2>Bring your Player’s Handbook</h2><p>The booklet uses the Fighter table and PHB equipment rules but does not reproduce them. It references Luck, chips, and death checks without defining their complete procedures. This site does not invent missing rules. Confirm those procedures and any ambiguous source wording with your DM.</p><p>The character workshop calculates ability modifiers, standard Fighter proficiency (+2 at levels 1–4, increasing by 1 every four levels), subclass save DC, hit die, and Fast / Slow speed adjustments. Snake’s default initiative includes proficiency. Enter HP, AC, resource uses, equipment bonuses, and other modifiers yourself. New features unlock at the levels specified in the booklet.</p><h2>Artwork & provenance</h2><p>The parchment texture is extracted from page 1 of the supplied v1.14 PDF. “Last of the Free,” the original battlefield illustration, was generated for this project with the built-in image-generation tool, using a brief inspired by the hand-painted military fantasy atmosphere of <em>Myth: The Fallen Lords</em>. It is new artwork, not an extracted game asset. <a href="docs/art-direction.md">Read the art brief and asset manifest</a>.</p><h2>Your character stays with you</h2><p>The workshop stores one draft in this browser’s local storage. Export JSON backups to keep multiple Brothers or move between devices. PDF generation runs in your browser; character data is not uploaded to a server. PDF files use genuine AcroForm fields and remain editable. Use a compatible PDF editor to change and save them; edits in the PDF do not sync back into the website.</p><h2>Project</h2><p><a href="https://github.com/kartoonstew/BCPrpoject" target="_blank" rel="noopener">Source code on GitHub ↗</a>. Built for the Black Company campaign. The supplied source material and its artwork retain their respective ownership.</p></div>`;}
function notFound(){main.innerHTML='<div class="page-wrap empty"><h1>Lost on the road.</h1><p>This entry could not be found.</p><a href="#rules" class="button">Return to the index</a></div>';}
function render(){const {path}=route();document.querySelectorAll('[data-nav]').forEach(a=>a.classList.toggle('active',path.startsWith(a.dataset.nav)||(a.dataset.nav==='subclasses'&&path.startsWith('class/'))));if(path==='home')home();else if(path==='rules')rules();else if(path.startsWith('rule/'))rule(decodeURIComponent(path.slice(5)));else if(path==='subclasses')subclasses();else if(path.startsWith('class/'))classDetail(decodeURIComponent(path.slice(6)));else if(path==='sheet')sheet();else if(path==='about')about();else notFound();document.title=`${path==='home'?'The Black Company':path==='sheet'?'Character workshop':path==='rules'?'Rules index':path==='subclasses'?'The brothers':path.startsWith('rule/')?record(decodeURIComponent(path.slice(5)))?.title||'Field manual':'Field manual'} — The Black Company`;window.scrollTo(0,0);}
async function init(){try{const response=await fetch('data/rules.json');if(!response.ok)throw new Error('Rules could not be loaded');DB=await response.json();character=defaults();try{const stored=localStorage.getItem('bc-character-v1');if(stored)character=sanitizeCharacter(JSON.parse(stored),DB.classes.map(c=>c.name),DB.records.filter(r=>r.category==='Traits').map(r=>r.title),DB.records.filter(r=>r.category==='Quirks').map(r=>r.title));const saved=JSON.parse(localStorage.getItem('bc-saved-rules')||'[]');if(Array.isArray(saved))bookmarks=new Set(saved.filter(id=>record(id)));}catch{/* Keep defaults when storage is unavailable or corrupt. */}render();window.addEventListener('hashchange',render);document.addEventListener('keydown',e=>{if(e.key==='/'&&!['INPUT','TEXTAREA','SELECT'].includes(document.activeElement.tagName)){e.preventDefault();const input=document.querySelector('input[type="search"]');if(input)input.focus();else{location.hash='rules';requestAnimationFrame(()=>document.querySelector('#rules-search')?.focus());}}});}catch(e){console.error(e);main.innerHTML='<div class="page-wrap empty"><h1>The Annals could not be opened.</h1><p>Reload the page to try again, or read the original rulebook.</p><a class="button" href="BC%20PHB%201.14.pdf">Open rulebook PDF</a></div>';}}
init();
